## Iris 出し先別の書き出し + 字幕の「隠れる帯」根治（2026-08-09 深夜/実行10:44・本番済 91601a3 / 696c083）

- ✅ **書き出せる形が 9:16 の1種類しかなく、しかも字幕が投稿すると隠れることを誰も教えていなかった のを根治**
  - 症状①: `IrisReelStudioMinimal`（＝かんたんモード＝**既定**）の書き出しは `OUT_W/OUT_H = 1080/1920` 固定で、**出し先を選ぶ場所がどこにも無かった**。フィード（4:5 がいちばん大きく出る）／正方形（並びで切られない）に出したい人は、9:16 を出して Instagram 側で切らせるしかなかった。「出し先別の書き出し」は 2026-08-07 の解像度根治の「残り」として1度書かれたまま2日間未着手（nightly-planner の 04:40 枠が Mac sleep で空振り）。
  - 症状②: 9:16 は Instagram のアプリが上（名前・音源）と下（本文・いいね・コメント・返信欄）を重ねる。**字幕の既定位置 `captionY = 0.78` はちょうど下の帯に食い込む**。しかも **Iris のプレビューでは全部きれいに見える**ので、隠れていることは投稿するまで分からず、投稿後も「反応が悪い」としか見えず原因に到達できない。判定に使える情報（折り返し規則・文字サイズ・yRatio）は全部 `drawAt` の中にしか無く、どこからも測れなかった。
  - 実装: **`src/iris/reelSafeArea.ts` を新設（計算だけ・DOM も canvas も触らない）** ＝ `REEL_DESTINATIONS`（9:16/1920・4:5/1350・1:1/1080。**幅は 1080 固定で高さだけ変える**＝字幕サイズが `canvas.width / OUT_W` で決まるため、幅を動かすと出し先ごとに文字の大きさが変わる）・`REEL_SAFE_TOP=0.13`/`REEL_SAFE_BOTTOM=0.22`・`wrapCaptionLines`（**折り返しの正本をここに一本化し `drawAt` から使う**＝別々に持つと「警告が出ないのに実際は隠れている」が起きる）・`captionBox`/`captionHiddenSide`/`safeCaptionY`。UI 側 ①書出ステップに出し先3択（44px 以上・比率の小さな図つき・why を一行）②`previewHeightFor` でプレビュー canvas と電話枠の `paddingTop` を同じ比率へ（切替時に canvas の height 属性が変わって中身が消えるので、静止描画 effect の deps に `previewH` を追加＝真っ黒にしない）③`startRecord` は `canvas.height = dest.h`、`finishExport` は `exportOutHRef` の高さのプレビューへ戻す（**9:16 固定で戻すと 4:5 の人だけ書き出し後に縦長のまま残る**）④出来た動画の実寸判定を `outSize.w >= OUT_W && outSize.h >= dest.h` に ⑤**隠れる帯のガイドは DOM のみ**（canvas に描かない＝動画に焼き込まれない）・録画中は消す ⑥`hiddenCaptionCount` を実測して「◯枚だけ、見える位置へ動かす」（隠れていない字幕は 1px も動かさない・文字は一文字も削らない）⑦結果画面に「同じ素材を、別の形でも出す」＝一度書き出した後も戻らずに形を変えられる。
  - 判定（375px・写真2枚を実注入して本番同等の画面で実測）: 比率 **0.562 / 0.800 / 1.000**（切替後に絵が消えないことも画素で確認）。「フィード（縦長）」で書き出した**実ファイルが 1080×1350**（プレビュー 360×450 →録画中 1080×1350 →復帰 360×450）。既定位置の字幕2枚で **警告が出る → ボタンで警告が消え captionY 0.78→0.75**。4:5/1:1 と字幕未入力では**警告 0**。横スクロール0・consoleエラー0・タップ 44〜80px・コントラスト 帯の説明 **18.2:1**（写真の上に直接置かず自前の濃い面 `rgba(10,10,15,0.88)` を持たせた／9.5px→10.5px）・直すボタン 5.02:1・選択中カードの白文字 4.61:1（グラデ最明部で実測）。tsc 0 / build 緑 / vitest **129件緑**（`reelSafeArea.test.ts` **18件を新設**＝比率・折り返し・「動かした後は必ず隠れなくなる」「隠れていない字幕は動かさない」「安全帯より背が高い時は文字を削らず中央」を固定）。本番の実チャンク `IrisReelStudioMinimal-C9KcQ0bC.js`（122,050 bytes・**ローカルビルドとハッシュ一致**）に新文言7つを直接確認・`/iris` 200・SSO リダイレクト無し。
  - **残り**: 上下の帯（13%/22%）は**目安**で、端末や表示で前後する（画面でも目安としか名乗っていない）。実機 iPhone は未（Chrome 375px まで）。長さ（尺）の出し先別の上限案内は未実装＝比率のみ。
  - なぜ大事: Iris が最後に渡すのは「投稿する動画」そのもの。形が1つしか無いのは機能不足だが、**字幕が隠れることを黙っているのは渡し物が壊れているのに言わない**種類の壊れ方で、プレビューでは常にきれいに見えるため**本人には一生気づけない**。

## 別件で見つけた赤（Iris とは無関係・未着手）

- ⚠️ **`src/data/appleHealthImport.ts`（Prism「カラダ」の Apple ヘルスケア取り込み）の vitest 3件が赤**。`appleHealthImport.test.ts` の自己完結型 Record・metadata つき Record・その混在のいずれでも **`days.length` が 0**（＝標準的な書き出しファイルを渡しても1日ぶんも読めない）。今回の変更より**前から**赤で、`a4f39e3` 時点の worktree でも再現。取り込んだつもりで何も入らない＝ユーザーには気づけない種類。

## 主ボタンの文字が面の側で読めるようになった（2026-08-09 深夜・本番済 ba74471）

- ✅ **アクセント色でベタ塗りした面の上の黒文字が、全画面で AA 未達だったのを根治**
  - 症状: 主ボタン（コピー・追加・保存・完了・「今日の一手」を実行 …）は **128 箇所すべて** が
    `background: persona.accentColor, color: '#0a0a0f'` ＝アクセント色の面にほぼ黒、で固定されていた。
    明るいアクセント（黄 11.83 / 緑 10.27 / 水色 8.14）なら通るが、中〜暗のアクセントでは黒が沈む:
    **#7C3AED 3.47 / #9333EA 3.67 / #DC2626 4.09 / #6B7280 4.09 / #6366F1 4.42**。
    **#9333EA は Prism のブランド紫そのもの**（デモの音楽スクールのペルソナもこれ）＝
    **いちばん押してほしいボタンの文字が、いちばん読めない**状態だった。
    しかも「黄色のペルソナを選んでいる人」には一生見えないので、本人の環境では気づけない。
  - なぜ readableInk では直せなかったか: `readableInk` は「テーマの紙（白/黒）の上に置く文字」用の式で、
    テーマの反対方向へ寄せる。ここは **面がアクセント色そのもの**なので、面の明るさから白黒を決める必要がある。
  - 実装: `src/lib/accentFace.ts` に `onAccent` / `onAccentInk` / `onAccentGradient` を新設し、
    **面と文字を必ず対で返す**（`style={{ ...onAccent(persona.accentColor) }}`）。方針は「通っている色は触らない」:
    ①黒が 4.5 通る明るい色は **黒のまま＝見た目を変えない** ②黒が落ちる色だけ **白文字**にする（面はブランド色のまま＝紫が濁らない）
    ③黒でも白でも届かない中間色（#6366F1 黒4.42/白4.47）だけ、**色相・彩度を変えずに面をずらす**（hover の `brightness(1.08)` 後も 4.5 残る所まで）
    ④hex で書かれていない色（`var(--accent)`・rgba・グラデ）は判定をあきらめ **これまでの黒のまま返す**＝勝手に白にして明るい面を壊さない。
    `accentFace.ts` に寄せたのは、白文字前提の `onAccentFace` と出どころを 1 つにするため（`ink.ts` は別の役割なので触っていない）。
  - **ついでに実測で見つかった同じ壊れ方**:
    - **全画面のナビ（選択中タブ）** が `#2E6FFF→#8E5CFF` に白文字で **4.10** → `whiteSafeGradient` で **4.61**
    - `.cp-btn-primary` の既定面 `--prism-creative` #8E5CFF が白文字 **4.10** → `--prism-creative-face` **#8149FF** を新設（hover 後 4.51）
    - 音楽スクールLPの主CTA は紫→金グラデで、黒が紫側 **3.67**。白にすると金側が 1.67 で落ちる＝どちらの文字色も通らない。
      **金は格を出している側なので暗くせず、紫だけ明るくする** `darkSafeGradient` を追加（`whiteSafeGradient` の裏返し）
    - `OnboardingWizard` は **白文字前提で暗くした面（`accentFaceBg`）に黒アイコン**を置いていた → `accentFaceInk` へ
  - 判定（本番と同じ 375px・紫 #9333EA のペルソナを作って実測）:
    「もう一度ためす」**3.67 → 5.38**（面は #9333EA のまま＝ブランド色を変えずに直した）。
    ホーム/機能/記録/会社/つながる/カラダ の **6画面で「アクセント面の上の黒文字」の AA 未達 0件**・
    横スクロール 0（docSW=375）・console エラー 0。
    tsc 0エラー・build 緑・vitest **129件緑**（`onAccent.test.ts` 9件を新設。
    **ペルソナ色をソースから自動収集する**ので、あとから足した色も検査対象に入る＝
    「一覧を手書きして更新し忘れ、テストは緑なのに読めないボタンが増える」を防ぐ）。
  - **残り（別スライス・次サイクルの1件目に向く）**: **「白文字 × ブランド色のベタ面」が同じ壊れ方でまだ残っている。**
    本番と同じ画面で実測した分だけで **#06C755「▶ ループを回す」2.26 / #E879F9「リサーチして生成」2.46 /
    #A78BFA「ギルドに改善アイデアを送る」2.72 / #4285F4「Google カレンダーと連携する」3.56**。
    道具（`whiteSafeFace` / `whiteSafeGradient`）は既にあるので、あとは同じやり方で一掃するだけ。
    ほかに未着手: ①`FocusHero` の実行中ボタンは `opacity: 0.7` を掛けており、
    中の文字のコントラストも一緒に落ちる（CSSの色を見る監査は全通りする種類の罠）
    ②アクセント色を**暗い地の上で文字に使っている**側（#9333EA 3.58 / #6366F1 4.31 / #E1306C 4.44）は
    `accentInkOnDark` の担当でこれも未適用 ③実機 iPhone での確認は未（Chrome 375px エミュレーションまで）。
  - なぜ大事: 機能の不足ではなく **一番押してほしいボタンが読みにくい**という、売り物の手触りそのものの不具合。
    しかも「自分のペルソナ色では起きない」ので、オーナーにも当人にも気づけない種類の壊れ方だった。

- ⚠️ **別件（この作業では触っていない）**: `src/data/__tests__/appleHealthImport.test.ts` の **3件が HEAD の時点で既に落ちている**
  （`a4f39e3` でも同じく失敗を実測）。カラダ画面の Apple Health 取り込みが **0日** を返している疑い＝
  「取り込んだのに何も増えない」になっている可能性がある。要調査。

## Iris コラボ画面の「作り話」根治（2026-08-08 深夜・本番済 45443b4 / 83e6dcc）

- ✅ **実在しない人が「実在の相手」として3経路で出ていたのを根治**
  - 症状①: 募集ボードは初回に架空の3人（`@hana_cosme` / `@tabi_noa` / `@mika_fashion`）の募集を localStorage に seed していた。各カードに「チャット」があり、空状態は「下の入力欄に書くと、**この相手とのやりとり**がここに残ります」と書いていたが、保存先は自端末のみ＝**相手には永久に届かない**。リアクションはカラー絵文字4種（絵文字禁止ルール違反）。
  - 症状②: `recommendCollabPartners` のシステムプロンプトが `"exampleHandle": "@dummy_handle (実際にいそうな架空のハンドル)"` を**AIに作らせて**おり、「+ 候補に追加」が `partnerHandle: rec.exampleHandle` でそれを相手として保存。フォールバックは `@${cat}_creator_${i+1}`。その相手に **`IrisDmDraftModal` で DM 下書きまで作れた**＝存在しない人にDMを送りに行く導線が完成していた。
  - 症状③: `computeAiMatch` が `Math.random()` で 40〜99 を作り、カード右上に **`92%` のようなパーセントで表示**。測っていない値を「測った顔」で出していた。
  - 実装: ①seed 廃止 + `stripLegacySeedPosts` で読み込み時に `seed-*` を除去して保存し直す ②型/プロンプト/フォールバックから `exampleHandle` を削除、プロンプトに「実在しない人名・@ を書くな」を明記、**AI応答に混ざった `@xxx` は正規表現で捨てる**。候補は `partnerHandle: ''` で追加し、`PartnerSlot`（「相手はこれから探す」+ 実在するハッシュタグ検索リンク3本 + 44px の入力欄）を出す。`launchDmDraft` は相手未定なら理由付きで止める。`clearFabricatedPartners` で保存済みの `@{cat}_creator_{n}` を空へ戻す ③`computeCategoryFit` に置換＝**同じ/近い/別ジャンルの言葉 + 形（角丸差） + 根拠文**の3点セット、決定的（同入力→同出力）④募集ボードを「自分の下書き」と明示（「他の人には届きません」を先頭に）、死んだチャットと絵文字リアクションを削除し「文章をコピー」（実クリップボード）+「Instagram を開く」+ 空状態3点セットへ。
  - 判定（本番 375px・旧版の壊れた状態を仕込んで Before/After 実測）: 架空3人が **画面からも localStorage からも消える（3件→0件）**、`@cosme_creator_1` → **「相手はこれから探す」**（保存も `''` へ）、DM下書きは **「先に相手を決めてください」で停止** → 名前を入れると保存されて開く、コピーは実文面がクリップボードへ、**パーセント表示 0**。tsc 0 / build 緑 / vitest **105件緑**（`collabNoFakePeople.test.ts` 9件を新設＝乱数に戻したら落ちる）。横スクロール0・consoleエラー0・主要操作 44px 以上で `elementFromPoint` 到達可・コントラスト 4.99〜16.6（自分で足した説明文が inkSoft 3.90 だったので ink 5.47 に是正）。
  - **残り**: AI が自由に考えた架空ハンドル（`@{cat}_creator_{n}` 以外の形）が既に保存されている分は、**ユーザーが自分で打った実在アカウントと見分ける手がかりが無い**ため消していない（消すと本物を消す risk）。今後は生成されないので自然に無くなる。
  - なぜ大事: 機能不足ではなく**画面が嘘をついている**種類の壊れ方。しかもユーザーからは「返事が来ないな」としか見えず、**自力では原因に到達できない**。月7千円を頂く商品の信用の根に当たる。

## 「今日の一手」タップ→成果→日報 の silent fail 根治（2026-08-08 深夜・本番済）

- ✅ **「動いた量に記録しました」と出しながら、実際には1件も記録していない入口が2つあった のを根治**
  - 症状: 成果物カードの「◯◯が納品 · 今週の『動いた量』に記録しました」は**無条件に表示**されていたが、日報への記録は**親が `onComplete` を渡した時だけ**行われていた。渡していない入口＝**チャット指令(`App.tsx`)と焦点モード(`FocusHero.tsx`)**では記録が0件のまま、画面だけが「記録しました」と言っていた。さらに `saveArtifact` / `cxoDeliverables.saveAll` は容量超過の例外を握りつぶして void を返しており、保存に失敗しても「自動保存済み」と出続けた（＝閉じたら成果物は消える）。
  - 実装: ①記録を `InlineActionExecutor` 自身に1本化（`runDelivery`）＝どの入口から開いても必ず記録する。親3か所(`TodayBrief`/`AgentTeamMonitor`/`DigitalCompanyHero`)の重複 `logDeliverable` は削除し、代わりに `cxo` / `logSource` を props で渡す（二重記録で件数を水増ししない）②`saveArtifact` を `boolean` 化＋容量超過時は 50→20→8→3→1 と古い分を捨てて詰め直し、**最後に読み直して実在を確認**してから返す ③`logDeliverableChecked` を新設（`saveAll` も MAX→200→50→10→3→1 で再挑戦＋読み直し確認）。**残らなかった時は `core:deliverable-added` を出さない**＝他画面が幻の1件を数えない ④画面は実測結果だけを言う＝記録できた時だけ「記録しました」、失敗時は理由＋「もう一度残す」(44px)＋「先にコピーで控えて」、フッタは「自動保存済み」→「未保存」。
  - 判定（375px・AI応答だけ固定にした実測ハーネスで Before/After）: 親が `onComplete` を渡さない使い方で **日報 0件 → 1件**（Before は0件なのに画面は「記録しました」と表示していることまで実測）。容量いっぱいを再現すると **日報0/成果物0 + 「この端末に残せませんでした」+「未保存」** が出て、「もう一度残す」で **0→1件** に復帰。tsc 0エラー・build 緑・vitest **96件緑**（`honestDeliveryPersistence.test.ts` 6件を新設＝保存の真偽・古い分を捨てて最新1件は残す・1件も入らない時は false を固定）・375px 横スクロール0・consoleエラー0。
  - **ついでに直した自分の新UIの読めなさ**: 生の琥珀色 `#F59E0B` は明るいテーマの面に対し **1.55:1** で読めなかった → `readableInk` で **5.9〜6.8:1**。救済ボタンをペルソナ色の面＋`#0a0a0f` にすると紫で **3.67:1** だった → ペルソナ色に依存しない `#B45309`＋白の **5.02:1**。ダークでも 8.3〜11.9:1 を実測。
  - 残り: **既存の `btnStyle('primary')`（「コピー」「もう一度」）は `background: accent` + `color:'#0a0a0f'` のままで、紫ペルソナで 3.67:1**＝文字コントラスト恒久ルール違反が残っている。全ペルソナ色で効く直し方（`readableInk` 相当を面側に入れる）が要るので別スライス。実機 iPhone での確認は未（Chrome 375px エミュレーションまで）。
  - なぜ大事: Prism は「役員がこれだけ働いた」を見せて月額を頂いている。その**記録そのものが入口によっては0件**で、しかも画面は「記録しました」と言い切っていた。機能の不足ではなく**信用の不具合**で、しかも本人には気づけない種類の壊れ方だった。

## Iris 書き出し解像度 根治（2026-08-07 深夜・本番済 ca5275a）

- ✅ **かんたんモードで書き出したリールが 360×640 の粗い動画だったのを 1080×1920 に根治**
  - 症状: `IrisReelStudioMinimal`（＝かんたんモード＝**既定**）の `startRecord` は、プレビュー用に 360×640 で描いているキャンバスを**そのまま `captureStream`** していた。出来上がる動画は 360×640 で、Instagram 側で 3 倍に引き伸ばされて字幕のフチがにじむ。**作った本人には気づけない**（スマホのプレビューでは小さくてきれいに見え、粗さが出るのは他人が見た時）。詳細エディタ側（`IrisReelStudio`）は元から `canvas.width = OUT_W` していたので、**多数派が使う側だけ**が粗かった。
  - 実装: `src/iris/IrisReelStudioMinimal.tsx`。①`startRecord` で `captureStream` を掴む**直前だけ** `canvas.width/height = 1080/1920` に上げ、1 コマ目を先に描いてから録画（空のキャンバスを掴むと冒頭が黒くなる）②`finishExport` で必ず 360×640 に戻す＝**成功・失敗・時間切れ・描画連続失敗のどの経路もそこを通る**（1080 のまま放置するとプレビューが 9 倍重くなる）③静止描画の `useEffect` に `!recording` を足し、書き出し中に**同じ絵を毎コマ 2 回描いていた**のをやめた（解像度を上げたぶんを相殺）④出来た動画の実寸を**出来たファイル自身から**読む（`<video>` の `loadedmetadata` → `videoWidth/Height`）＝カタログ値を名乗らず毎回測り、推奨に届かなければその事実を出す ⑤実際に描けたコマ数から本当の fps を出し、20 コマ/秒 未満なら理由と直し方を出す（黙ってカクついた動画を渡さない）。警告面は既存の amber tinted panel ＋ `bg.ink`（面と文字をテーマトークンで固定＝白面に薄い文字を作らない）。
  - 判定（本番と同じ画面に写真2枚を入れて実測）: プレビュー 360×640 → **書き出し中 1080×1920** → 終了後 360×640 に復帰。**出来た動画 360×640 → 1080×1920**。同じキャンバスで旧経路（resize せず `captureStream`）を再現すると 360×640 のままになることも実測＝差分の証拠。1 コマの描画コスト **0.43ms → 0.61ms**（30fps の予算 33.3ms に対し余裕）。tsc 0 エラー・build 緑・vitest **90 件緑**・375px 横スクロール 0・console エラー 0。本番の実チャンク `IrisReelStudioMinimal-DpI3JTPU.js`（116,747 bytes の実 JS）を直接落として新文言 3 つを確認（`/iris` 200・SSO リダイレクト無し）。
  - 残り: **出し先別の書き出し（フィード 4:5／ストーリー）は未実装**＝いまも 9:16 の 1 種類だけ（下の「スマホだけで最後まで終わる」①と同じ項目）。実機 iPhone での書き出しは未実測（Chrome 375px まで）。
  - なぜ大事: Iris が渡す最終成果物は「投稿する動画」そのもの。そこが推奨サイズの 1/3 で出ていたのは、機能の不足ではなく**渡している物の品質の不具合**。しかも本人が検知できない種類の壊れ方だった。

## honest-numbers 根治（2026-08-07 深夜・本番済 d0c651b）

- ✅ **数字が「途中の値のまま固まって嘘の金額を見せる」経路を全部ふさいだ**
  - 症状: カウントアップは `requestAnimationFrame` で動くが、rAF は**裏タブ / 画面ロック / 省電力で発火しなくなる**。すると売上・KPI・取り戻した時間が **0 や途中の値のまま固まり、本当ではない金額**が表示され続ける。お金を頂いている根拠の画面が嘘をつく状態だった。
  - 実装: `src/hooks/useHonestCountUp.ts` を新設し、同じ実装がコピーされていた **5 か所を全部寄せた**（`DailyReport.tsx` / `visualFx.tsx` の `CountUp`＝KPI 実績・Iris 売上・売上台帳 / `CountUp.tsx` / `PrismFlow.tsx` / `LiveSignupCounter.tsx`）。守るのは4つ: ①動きを減らす設定なら即最終値 ②画面が裏にある間は演出せず即最終値 ③演出中に裏へ回ったらその場で着地 ④**rAF が一度も来なくても時間切れで必ず最終値へ着地する安全網**（`setTimeout` は裏タブでも間隔が伸びるだけで必ず発火する）。数字そのものは一切いじっていない＝出る値は常に実データのまま。
  - 判定（実測・rAF を一度も発火しない状態を作って計測。正しい数字 12345）: **修正前は 4 秒待っても 0 のまま（永久に 0）→ 修正後は 12345 に着地**。裏で開いた時は rAF 呼出 **0 回**で最初から 12345（演出そのものを始めない）。演出中に裏へ回った時もその場で 12345。tsc 0 エラー・build 緑・`/lp/sme` 375px 横スクロール0・console エラー0。本番の実ページが `assets/useHonestCountUp-DhmmQePQ.js` を実際に読み込んでいることまで確認（ハッシュはローカルビルドと一致）。
  - 残り: 「動きを減らす」を**あとから切り替えた**場合は、その画面を開き直すまで前の設定のまま（開き直せば効く）。実機 iPhone での画面ロック→復帰の往復は未実測（rAF を殺した再現テストまで）。
  - なぜ大事: Prism は「AI がこれだけ働いた・これだけ稼いだ」を見せて月額を頂いている。その数字が黙って 0 になるのは、機能の不具合ではなく**信用の不具合**。

## 競合から盗む（2026-08-09）
> 出典: Raycast＝「**ひとつのキーを押すと、いまいるアプリを離れないまま浮かぶ窓が出て、書く・要約する・訳す・訊くが終わる**。別のチャット画面へ移らなくていい」「1週間使うと指が覚えて、以前どれだけ引っかかりの中で生きていたか忘れる」「機能が積み上がっても圧に見えない。ひとつひとつが十分よくて、**考えなくなる**」（thesunrisedigest.com／blog.omerh.me／nubiapage.com 2026年レビュー）。Later＝「**投稿する前に、プロフィールに並んだ時の見え方**を見て、掴んで動かして順番を決められる。予約したぶんも並びに入る」（heropost.io／creatorstackclub.com 2026年比較）。
> ※先週までとの違い: 7-26/8-02 とは**別の軸＝①離れないで済むこと ②未来ぶんの並び**。体験の移植のみ・丸コピー禁止・嘘数字禁止。Prism = パープル#9333EA／Iris = ピンク#FF6B9D。

### Prism（司令塔）
> 現状確認（`src/components/CommandPalette.tsx` 1587行 実読）: コマンドバーは**すでにある**し、「よく使う・最近」も `core_cmd_palette_recent_v1` で**すでに効いている＝作らない**。問題は出口で、**18個の `onRun` が軒並み `onClose()` を呼んで画面ごと移動する**。つまり「訊く→答えが出る」ではなく「訊く→**別の場所へ連れて行かれる**」。

- ⬜ **答えをコマンドバーの中で見せて、いた場所から動かさない（Raycastの"浮かぶ窓"を移植）**
  - なぜ気持ちいい: Raycast が速く感じる本当の理由は処理の速さではなく、**戻ってこなくていい**こと。作業の途中で開いて、答えを受け取って、閉じれば元の続き。Prism はいま、ひとこと訊くたびに見ていた画面を失うので、**訊くこと自体をためらう**。
  - どこにどう: 「答えが1つ返るだけ」の項目（要約・今日の状況・数字の確認・AIへのひとこと）を**その場で下に開く**形にする。画面遷移は**やめるのではなく、選べるようにする**（結果の右下に「この画面を開く」を小さく1つ）。**閉じたら元の画面のスクロール位置に戻る**。既存の遷移する項目（設定・請求など）はそのままでよい。判定（§0.5）: 「よく使う」上位5つのうち、**答えを見るだけで済むものが何個、画面遷移0回になったか**を数える。
- 🟡 **同じことができる入口が何個あるかを、まず数えて1つに寄せる（Raycastの"入口はひとつ"を移植）** — 2026-08-13 に棚卸しを実施（`317963f`）。
  **数えて分かったのは「入口が多い」ではなく「肝心の入口が iPhone に1つも無い」だった。**
  375px 実測: ホームに浮いている押せるものは3つ（COMMAND タブ / 音声でタスク予約 / ダークモード）＋下部チャットドック。
  そのどれもコマンドバー（`CommandPalette`・80件）を開かない。COMMAND タブが開くのは別物の**コマンドセンター**。
  コマンドバーの入口は `.prism-cmdk-entry`（`hidden md:flex`）と浮きピル（`検索・操作メニューを開く`）の2つだが、
  **浮きピルは index.css で `display:none`**（2026-07-17「ボタンが多すぎる」の間引きで、「右端の COMMAND タブと重複」という
  **誤った前提**で消されていた）。結果、スマホでは **⌘K キーでしか開けない＝キーボードの無い iPhone からは 80 件すべてが到達不能**。
  対処: 浮きボタンを増やさず、☰ の先頭に「探す・操作する」を置いた（∞タップ → 2タップ）。
  あわせて `成果物 / 統合脳 / 役員日報` をコマンドバーへ（80→83件）。これまで入口は右下の名前の無い「＋」1つだけで、
  名前で探しても出てこなかった。**「＋」は消していない**（消すと2タップより遠くなる）。
  - **残り**: ①「探す・操作する」は ☰ の中で2タップ。1タップにするなら下部チャット欄に打った文字で
    コマンドも当てる（Raycast の"入口はひとつ"の本丸）。②`.prism-cmdk-entry` は `md:flex` だが
    768〜1099px では別の理由でも消される＝**タブレット幅でも入口が無い**（未確認・要実測）。
    ③浮きボタンの数はまだ3つ。「音声でタスク予約」と下部ドックの「通話（声で話す）」は
    どちらもマイクで、意味の違いが画面から読めない。
  - なぜ気持ちいい: Raycast は入口が1つしかないから指が覚える。Prism には `CommandPalette` / `CommandRail` / `CommandCenter` / `CommandTowerHub` / `BottomChatDock` / `AISidebar` と、**名前からして似た入口のファイルが6つ**ある（実在を確認）。どれがどれか覚えられない入口は、**あることを忘れられて使われない**（`env_disabled_autostart_orphans_feature` と同型）。
  - どこにどう: **まず実装ではなく棚卸し**。①iPhone幅の実画面で、同時に見えている入口を数える ②それぞれから「AIに訊く」に到達できるか試す ③重複しているものを**1つに寄せる（消すのではなく、同じ入口の中へ畳む）**。到達できない入口が見つかったら、それは機能が迷子になっている印なので先に直す。判定: 画面上の入口の数と、「AIに訊く」までのタップ数のBefore/After。

### Iris（Instagram運用）
> 現状確認（`src/iris/IrisGridPreview.tsx` 実読）: Later の並びプレビューは**移植済み**だが、並ぶのは `loadPostedGrid(8)` ＝**投稿済みの8本＋いま作っている1枚だけ**。予約ずみの投稿は1つも入らない。予約は別画面（`IrisPostQueueView.tsx`・`suggestNextSlot` でよく伸びる時間帯へ自動割当まで済み）にあり、**「これから出る順番でどう見えるか」はどこにも無い**。

- ✅ **予約ぶんも並びに入れて、「来週のプロフィール」を先に見せる（Later の視覚プランナーの続き）** — 2026-08-12 完了（`coverGrid.ts` に `loadPlannedGrid`／`plannedDateLabel` を追加、`IrisGridPreview.tsx` が予約→投稿済みの順で並べる）。375px 実測: 予約3件が **8/17・8/14・8/12** の順（いちばん先の予約が左上）でマスの右上に日付、そのうしろに投稿済み2件。下書きは並ばない・画像の無い予約1件は並べず「1件は画像がないので出していません」と正直に表示・**予約0件のときは見た目が1pxも変わらない**（実測）。テスト `coverGrid.test.ts` を 12→20 件に。
  - なぜ気持ちいい: Later を使う人が本当に見たいのは過去ではなく**これから並ぶ姿**。3枚続けて暗い写真、同じ色が2列、といった事故は**出す前**にしか直せない。Iris はいま出したあとにしか気づけない。
  - どこにどう: 並びプレビューの左上から**予約ずみを新しい順に差し込み**、投稿済みはその後ろへ。予約ぶんのマスの隅に**日付だけ**を小さく（「8/12」）。**予約が0件のときは今のまま**（架空の枠を並べない＝空っぽ3点セット）。読めない大きさの見出しを知らせる既存の判定（`gridLegibility`）は予約ぶんにもそのまま効かせる。
- ✅ **並びの中で順番を入れ替えたら、予約時刻もついてくる（Later の"掴んで動かす"を移植）** — 2026-08-14 完了（`src/iris/gridReorder.ts` を新設・`IrisGridPreview.tsx` の予約マスを押せる形に・`usePostQueue` が同じタブの書き換えも拾うように）。
  - **長押しドラッグではなく「押して掴む → 入れたい場所を押す」の2タップにした**理由: iPhone Safari は `<img>` の長押しで OS のメニュー（画像を保存）が先に出るうえ、ページのスクロールと取り合いになる。指の回数は長押しドラッグと同じ2手のままで、取りこぼしが無く、読み上げ・キーボードでも同じ順番で操作できる。
  - 実装で守ったこと: ①動かせるのは `scheduled`/`ready` の予約どうしだけ（投稿済み・下書き・見送りは `<button>` にしない＝掴めないことが形で分かる）②入れ替わるのは **`scheduledAt` だけ**（本文・画像・ハッシュタグは1文字も動かさない）＝よく伸びる時間帯の割当を壊さない ③移った先の時刻に合わせて `scheduled`/`ready` を付け直す ④保存先は予約リスト本体（`iris_post_queue_v1`）だけ・並び用のコピーを作らない ⑤同じタブでは `storage` イベントが飛ばないので `iris:post-queue-changed` を新設し `usePostQueue` にも配線（これが無いと予約一覧が古い時刻のまま残る）⑥**書けたかどうかを戻り値で見る**＝容量不足で保存できなければ「入れ替えました」と言わずに理由を出す ⑦「元に戻す」を10秒 ⑧掴んでいる最中の「動かすのをやめる」（行き止まりにしない）。
  - 検証: 375px 本番ビルド実測 — 予約3件のマスは 98×123px、1枚目を押して3枚目を押すと並びが `far/mid/near` → `near/mid/far`、予約リスト本体では `q_far` と `q_near` の時刻だけが入れ替わり `q_mid` と投稿済みは1文字も動かず、本文も元のまま。「元に戻す」で時刻・状態とも完全に復帰。投稿済みのマスは `<button>` になっていない。docOverflow 0・pageerror 0。単体テスト `gridReorder.test.ts` 12件（保存失敗時に成功と言わないことも固定）。
  - なぜ気持ちいい: Later の気持ちよさは、**考える場所と直す場所が同じ**こと。並びを見て「これは後だな」と思ったその指で動かせる。Iris は今、並びを見て気づいても、別画面の予約一覧へ行って時刻を打ち直す＝気づきが消える。
  - どこにどう: 予約ぶんのマスだけ**長押しして入れ替え**（投稿済みは動かせない・掴めないことが見て分かる形に）。入れ替えたら、**枠（時刻）はその場に固定して、中身だけが入れ替わる**（＝よく伸びる時間帯の割当を壊さない）。**指1本で動かせる大きさ**（マスは実測44px以上）。1回だけ「元に戻す」を10秒出す。反映先は `usePostQueue` の予約一覧そのもの（2か所で別々に持たない＝`env_same_count_computed_in_two_places`）。

## 競合から盗む（2026-08-02）
> 出典: Later＝「**貯めた素材の棚（Media Library）と、出す前に並びを見られる視覚プランナーが最大の強み**。ドラッグで並べ替えて、公開する前に見え方を確かめられる」「Buffer の下書きプレビューは1枚単位で、Later のような並びの確認はできない」（fahimai / heropost / creatorstackclub 2026比較）。Notion AI＝「**会議のメモが、既にある案件・顧客・タスクに繋がるから効く**（単体の要約ツールとの決定的な差）」「Connectors で外の道具まで1つの入口から探せる」「2026年1月から**画面を消しても録り続ける**」（tldv / eesel / beginnersinai 2026レビュー）。※先週(7-26)の CapCut「字幕スタイル」「スマホで完結」・Raycast「@メンションで外部実行」とは**別の軸**。体験の移植のみ・丸コピー禁止・嘘数字禁止。
> 現状確認（実読）: Iris は `IrisBestTime.tsx` で**自分の実績からの投稿時間はすでに実装済**（Laterのその軸は重複＝盗まない）。Prism は `MeetingMinutes.tsx` に**話者の区別もタスク提案もすでに実装済**（Notion AI がむしろ持っていない部分＝重複させない）。`CommandPalette.tsx` の横断検索は `personaKnowledge.slice(0, 50)` で**黙って50件で打ち切っている**。

### Iris（Instagram運用・ピンク#FF6B9D）
- ✅(2026-08-03 92d427b 本番済) **撮った素材が貯まる「棚」を作る（Later の Media Library を移植）**
  - 実装: `src/iris/reelStore.ts` に棚 (library) を新設 (DB v2)＋`src/iris/IrisAssetShelf.tsx` 新規。**根っこは保存側の壊れ方だった**: `clearReelStore` が assets を丸ごと clear し、`pruneReelAssets` が「いま編集中のリールで使っていない素材」を全部消していた＝棚を UI だけ足しても毎回空になる。①Blob は既存 assets、見出しは library に置き、掃除の keep 集合に棚の id を必ず足す ②棚から消えるのはユーザーが「整理」で選んだ時だけ (`deleteLibraryItems`)。編集中のリールが使っている素材は棚から外しても**実体を残す**（画面から素材が消える事故を防ぐ）③書き出し成功時だけ `markLibraryUsed` で「使った」印（`logIrisActivity` と同じ経路＝嘘の数字にしない）④端末の空きが8割超の時だけ「まだ使っていない古い素材 ◯件」を提案し、消す前に名前を見せて確認（黙って消さない）⑤空の棚は3点セット ⑥`navigator.storage.estimate()` が取れない端末では空き容量の行を出さない。かんたんモード・詳細モードの両方に設置。
  - 判定（実測）: 2本目を作り始めるまで＝**カメラロールを開く 1 回 → 0 回**（棚のタイルをタップ1回でリールに入る）。クリップを全部消した後に残る素材 **0 件 → 3 件**（375px 実測・リロード後も IndexedDB に3件）。tsc0・build緑・reelStore テスト9件緑（棚が掃除で消えないことを固定）・本番 `/iris` 200・横スクロール0・consoleエラー0。
  - 残り: 実機 iPhone での容量ひっ迫（8割超）の見え方は未実測（`storage.estimate` を差し替えた確認までは行っていない）。棚の並べ替え・検索は未実装（件数が増えた時の次スライス）。
  - なぜ気持ちいい: Later が写真系ブランドに選ばれ続けている理由は編集機能ではなく、**素材が1か所に貯まっていて、作る時に選ぶだけ**という状態。ネタ切れの正体は「素材がない」ではなく「どこにあるか分からない」。Iris のリール作りは**毎回カメラロールを開くところから**で、前に使った素材・使わなかった素材はどこにも残らない＝作るたびに毎回ゼロからやり直している（`IrisReelStudio` の素材ゼロ画面が毎回の出発点）。
  - どこにどう: リールスタジオに**「棚」タブを1つ**。①一度読み込んだ素材はサムネイルで棚に残る ②書き出したリールに使った素材には**使用済みの小さな印**（同じ絵を続けて使わないため）③棚からタップで即キャンバスへ。**保存はローカル（IndexedDB）＝アップロードしない**（通信ゼロ・費用ゼロ・プライバシー）。容量が近づいたら**古い未使用から順に消す前に必ず聞く**（黙って消さない）。棚が空の時は「まだ棚は空です／写真・動画を入れると、ここに残ります」＋押せるボタン1つ（空っぽ3点セットの型）。
  - 判定: 2本目以降のリールを作り始めるまでのタップ数を Before/After（毎回のカメラロール往復が消えるか）。棚から選んで作られたリールの割合。
- ✅(2026-08-03 7fc2eb8 本番済) **出す前に「並んだ時の見え方」を確かめる（Later の視覚プランナーを移植）**
  - 実装: `src/iris/coverGrid.ts` 新設（実データ収集＋読めるかの判定）＋ `src/iris/IrisGridPreview.tsx` 新規 ＋ `IrisCoverStudio.tsx` に組み込み。①カバー作成の下に **3×3 の並びプレビュー**。左上が今つくっている表紙（canvas を手が止まってから 300ms 後に1枚書き出す＝打つたびの書き出しをしない）、その後ろに**投稿済みの直近8本を新しい順**（予約リストで「投稿した」にした分＋Instagram 連携で取り込んだ分。同じ画像は1回だけ＝並びを水増ししない）②**1件も無い時は架空の見本を出さず**、理由と「予約リストを開く」（44px）を出す（空っぽ3点セット）③**並びの中での見出しの実寸を実測**して知らせる＝マスの幅を `ResizeObserver` で測り、実際に描いた文字サイズ × 縮小率で px を出す（推定しない）。8px 以上=読める／6-8px=警告／6px 未満=読めません ④プロフィールの並びは **4:5 で切り取られる**ので、9:16・1:1 は切れることを先に書く（プレビュー自体も 4:5 で切って表示＝実際に切れて見える）⑤**「文字を直す」で大見出しの欄に戻れる**（44px・スクロール後にカーソルを当てる）⑥Instagram 連携の取り込みに `thumbUrl` を保存（期限切れするので取り込むたびに差し替え、表示できないマスは落とす）。
  - **ついでに直した既存の不具合**: 見出しの自動縮小が**一度も効いていなかった**（`wrapLines` が4行に切り詰めた後の行数で判定していたため、必ず「4行に収まっている」と見えて1回目で break）。結果、4行に入らない文字は**黙って画像から消えていた**。切り詰める**前**の行数で判定するようにし、それでも入りきらない時は「見出しの終わり ◯文字が画像に入っていません」を赤字で出す（黙って消さない）。
  - 判定（375px 実測・投稿済み3件を入れて計測）: 並びを見るためにインスタを開く往復 **1 → 0**。見出しを長くした時の実寸 **8.7px → 7.9px** と、その場で警告が出ることを実測（39文字の見出しで自動縮小が効くようになった＝以前は 101px のまま）。黙って消えていた文字 **有 → 0**（縮小が効くようになり、それでも溢れる時は文字数を表示）。マス幅 98px・9マス・現在作成中の1枚＋実データ3件を実測。tsc 0エラー・vitest **67件緑**（`coverGrid.test.ts` 10件を新設＝実データ以外を並べない／新しい順／重複除去／壊れた保存で落ちない を固定）・build緑・横スクロール0（docSW=375）・consoleエラー0・タップ44px・本番チャンク `IrisCoverStudio-S77ftS4_.js` に新コード確認・`/iris` 200。
  - 残り: 並べ替え（ドラッグで並びを試す）は未実装＝いまは「今作っている1枚が先頭に入るとどう見えるか」まで。実機 iPhone での確認は未（Chrome 375px エミュレーションまで）。
  - なぜ気持ちいい: Later のプランナーが効くのは、**1枚だけ見ると良いのに、並ぶと崩れる**という失敗をなくすから。プロフィールを開いた人が最初に見るのは1本ではなく**並び**なので、そこを事前に見られるだけで「出す勇気」が出る。Iris は `IrisCoverStudio.tsx` でカバーを1枚ずつ作れるが、**それが自分のリール欄に並んだ時どう見えるかを見る場所がない**＝作った本人だけが1枚ずつ気に入っている状態。
  - どこにどう: カバー作成画面の下に**3×3の並びプレビュー**を出す（今作っているカバー＋**すでに投稿済みの直近8本のカバー**を実データで並べる。連携していない・投稿が無い時は**架空の見本を出さず**「投稿が増えると、ここに並びが出ます」と正直に書く）。文字の大きさ・色が**並ぶと読めなくなる**場合は小さく警告（並びの中では実寸が数pxなので、そこで初めて分かる）。**カバーは作り直せる**＝プレビューからそのまま編集に戻れる。
  - 判定: 「並びで見ると読めないカバー」の検出件数と、書き出し後にカバーを作り直した回数。並びを見るための往復（インスタを開いて確認する）が 1→0。

### Prism（AI司令塔・パープル#9333EA）
- ✅(2026-08-03 462c2ce 本番済) **録音が途切れても、そこまでが必ず残る（Notion AI の"画面を消しても続く"を、Web でできる形に移植）**
  - 実装: `src/lib/meetingDraft.ts` 新設 ＋ `src/components/MeetingMinutes.tsx`。①**文字が増えるたびに端末へ自動下書き保存**（localStorage・送信しない。保存の引き金を「segments が伸びた時」だけにして、1秒ごとに動く経過時間で書き込みが暴れないようにした）②開き直すと**復帰バナー**「前回の書き起こしが◯文字ぶん残っています／◯月◯日 H:MM まで保存されています」＋「続きから使う」「捨てる」（文字数は `draftCharCount` の実測値のみ・推定や水増しなし）。復帰後は **「▶ 続きから録る」（segments を消さない）** と「🎙 最初から」を選べる ③**黙って止まらせない**: `onend` の再起動が2度続けて戻れなかったら赤い1行＋「文字起こしを再開」（44px）を出す。`visibilitychange` で画面に戻ったら自動で入れ直し、止まっていた時だけ「画面が戻ったので再開しました」を出す ④録音前に**できないことを先に書く**「画面を消す・ほかのアプリに切り替えると、文字起こしは止まります（音声の録音は続きます）」＋非対応ブラウザには別文言 ⑤議事録をナレッジに保存できたら下書きは消す。
  - 判定: 途中で止まった時に残る文字数 **0 → そこまで全部**（85文字の下書きで復帰し、3段落と話者名2件の復元を実測）。「止まったのに気づかない」経路＝`onend` 再起動失敗の**黙って終了 1 → 0**。tsc 625ファイル0エラー・build緑・375px 実測（横スクロール0／consoleエラー0／タップ44px）・本番チャンク `MeetingMinutes-0WdkKa57.js` に新コード確認。
  - 残り: 実機 iPhone での画面ロック→復帰の往復は未実測（Chrome の 375px エミュレーションまで）。
  - なぜ気持ちいい: Notion AI が2026年1月に**画面ロック中も録り続ける**ようにしたのは、会議の書き起こしで一番怖いのが「終わってみたら何も残っていなかった」だから。信用できて初めて、人は道具に会議を預ける。Prism の `MeetingMinutes` はブラウザの音声認識（`webkitSpeechRecognition`）で動くため、**スマホで画面を消す・別のアプリに切り替える・電話が来る、で止まる**。しかも止まったことが分かりにくい＝1時間の会議が丸ごと消える事故になりうる（Web の制約上「消しても録り続ける」は実現できない＝**そこは正直に言い、代わりに失わない設計にする**）。
  - どこにどう: ①**書き起こしを一定間隔（例：30秒ごと・段落が確定するたび）でローカル保存**し、事故のあと開き直したら「**◯分ぶんの書き起こしが残っています。続きから録りますか？**」で復帰できる ②**認識が止まったことを画面で必ず知らせる**（無音が続いた時と、`onend`/`onerror` が来た時。赤い1行＋「録り直す」ボタン。黙って止まっているのが最悪）③録音中は「画面を消すと止まります」を**始める前に1行だけ**書く（できないことを先に言う＝あとで裏切らない）。※`webkitSpeechRecognition` 非対応の端末では**録音ボタン自体を出さない**（表示した機能は必ず動く）。
  - 判定: 途中で止まった時に残る文字数（Before は0）。「止まったのに気づかなかった」がゼロになること。実機（iPhone）で画面ロック→復帰の往復を実測。
- 🔶 **1つの入口で全部を探せて、答えに必ず「出所」が付く（Notion AI Connectors を移植）＋打ち切りを隠さない**（③打ち切りを隠さない＝完了・①②は残り）
  - ✅(2026-08-03 実装 `21d3c45`／**2026-08-04 09:50 本番反映済**。前日は Vercel 無料枠の1日100デプロイ枠切れで出せず、枠が戻った翌朝のデプロイ(`8c586aa` と同便)で出荷。本番 `main-BBJvWRuh.js` に「ほかに◯件あります」の文言を実確認) **黙って捨てていた「51件目以降」をゼロにした**。`src/components/CommandPalette.tsx`。①`personaKnowledge.slice(0, 50)` と `tasks.filter(!done).slice(0, 30)` を撤去＝**検索は必ず全件が対象**。これまでは51件目以降のナレッジ・31件目以降のタスクが候補配列に入っておらず、**在るのに「見つかりません」と出る**状態だった（探し物が見つからない最悪の型）②代わりに**画面に並べる数だけ**を `PREVIEW_CAP`（ナレッジ8・タスク8）で畳み、**隠した数は必ず出す**＝区分の見出しを「ナレッジ 8 / 60」（出している数 / 本当の総数）にし、末尾に「**ほかに 52 件あります・すべて見る**」（48px・区分の色）を置いた。押すとその区分だけ全件展開（`expandedCats`）。パレットを閉じると畳み直す ③「すべて見る」は候補配列に入れない＝キーボードの↑↓と「最近使った」を汚さない。
  - 判定（**ローカル dev の 375px 実測**・ナレッジ60件/未完タスク12件で計測。本番実機での確認は枠が戻ってから）: **検索できないナレッジ 10件 → 0件**（60件中51件目以降。IndexedDB の実配列に対し旧 `slice(0,50)` が `検証メモ52_ゼブラ` を候補に含まないことを実測 → 現行は同じ語で1件ヒット）。**黙って隠していた件数 52件 → 画面に出る**（見出し `ナレッジ 8 / 60`＋「ほかに 52 件あります」）。**最初に並ぶナレッジ行 60 → 8**（1画面あたりの雑音を減らし、必要な人は1タップで全件）。「すべて見る」を押すと `ナレッジ60`・行 79→131 に展開を実測。tsc 0エラー・vite build 緑・横スクロール0（docSW=375）・consoleエラー0・タップ48px。
  - 残り: ①検索対象に議事録・予約・受信箱を足す ②各行の出所チップ（いまは区分の見出しで出所が分かる状態まで）。予約(`bookingInbox`)・受信箱(`gmail`)は取得に通信が要るため、「新しい通信を増やさない」条件をどう満たすか（既に読み込み済みの時だけ載せる等）を先に決める必要がある。
  - なぜ気持ちいい: Notion AI が評価されているのは賢さより「**探す場所が1つしかない**」こと。会議メモも案件も同じ入口から出てきて、しかも**どこから来たか**が分かるので、そのまま信じて動ける。Prism の `CommandPalette` は知識だけを見ており、会議の議事録・予約・受信箱は別々の画面にある＝「あの話どこだっけ」で結局全部の画面を回ることになる。さらに `personaKnowledge.slice(0, 50)` で**51件目以降を黙って捨てている**＝「無い」と表示されるのに本当は在る（探し物が見つからない最悪の型）。
  - どこにどう: ①コマンドパレットの検索対象に**議事録・予約・受信箱**を足す（既にメモリ上にあるものだけ＝新しい通信を増やさない）②各行の右端に**出所チップ**（知識／会議／予約／受信箱）を付け、押すとその画面の該当箇所へ飛ぶ ③`slice(0, 50)` の打ち切りを**画面に出す**（「ほかに◯件あります／もっと見る」）＝黙って切らない。検索語が無い時の既定リストは今のままでよい。
  - 判定: 「知っているのに見つからない」検索語での命中率を Before/After。出所チップ無しの行が 0件。50件超のとき打ち切りが必ず表示されること。

## 競合から盗む（2026-07-26）
> 出典: CapCut＝「自動字幕は最もよく使われる機能／55以上の言語＋効果音の字幕もトグルで」「モバイル版が驚くほど機能的に完全＝スマホだけで完結する」「創作者が毎回くり返す仕事（切る・字幕・比率変更・書き出し）から摩擦を消したのが勝因」（nemovideo / bigvu / capcut.com 2026レビュー）。Raycast＝「MCPで @メンション記法により Notion 等の外部ツールに接続し、"このバグのIssueを作って"を実際に実行できる」「必要な結果だけを出す＝雑音がない」（producthunt / gadgetbond 2026）。※先週(7-23)のグリッドプレビュー・トレンド音源テンプレ・インライン編集・コマンドバー集中とは**別の軸**。体験の移植のみ・丸コピー禁止・嘘数字禁止。

### Iris（Instagram運用・ピンク#FF6B9D）
- ✅(2026-07-27 6cb3b46 本番済) **字幕を「出す」から「1タップで似合わせる」へ（CapCut の自動字幕＋スタイルを移植）**
  - 実装: `src/iris/IrisReelStudio.tsx`。①スタイル3種を**実物プレビュー**に（`CaptionStylePreview` が書き出しと同じ `drawCaption` を 216×384 の 9:16 キャンバスに再生＝見えたものが本番と一致。ポップは1語ずつ光るカラオケがそのまま動く／シネマ・クリーンは入場アニメを切り「いつ見ても文字が出ている」状態に＝フェード中で見えない瞬間を無くした／見本の文字は色見本と同じ考えで1.8倍＝親指幅では実寸だと数pxで違いが見えないため。画面外では `IntersectionObserver` で描画停止＋20fps間引き＝電池を無駄に使わない）。②**細かい設定を既定で畳む**（書体・大きさ・色・縁取り・影・出現＝`showCapAdvanced`、開閉ボタンは44px）＝3枚から選んで終われる人に設定の壁を見せない。③**効果音・言いよどみを1タップで消す**（`tidyCaptions` 純粋関数が `(音楽)(拍手)♪`・先頭の「えー/あのー」・空になった行を落とす。消した件数を必ず通知し「整える前に戻す」で1タップ復元＝黙って消さない）。
  - 判定（タップ数 Before→After）: 3スタイルを見比べる = 3回当てて毎回プレビューへ視線往復 → **0回（3枚が同時に動いて見えている）**。字幕を"見せられる状態"に = 手作業で効果音行を1つずつ削除 → **1タップ**。tsc該当0・build緑・本番 `/iris` 200・375px 実測（横はみ出し無し／選択切替／整える⇄戻す往復）。
  - なぜ気持ちいい: CapCutの自動字幕が最も使われるのは、文字起こしだけでなく**見た目のスタイルまで一発で決まる**から＝「読める・映える」までが1タップ。Irisは音声から字幕を作れるが、そこから先の見た目調整が手作業＝結局CapCutへ出て行く。
  - どこにどう: 字幕生成の直後に **スタイル3枚（太字白フチ／下部帯／1語ずつポップ）を実物プレビューで**並べ、タップで即適用。**効果音・間の無音は自動で除く**トグル1つ。文字サイズ/位置の細かい設定は隠す（段階的開示）。判定＝字幕が"見せられる状態"になるまでのタップ数をBefore/Afterで示す。
- 🔶 **スマホだけで最後まで終わる（CapCut の"モバイルが本体"を移植）**（部分完了）
  - ✅ 2026-08-02 8ec2162 本番済 **素材ゼロの最初の画面から、押しても何も起きない選択肢を33個消した**。375px 実測で開いた直後の押せるものが **55→22**（3.2画面→2.5画面）。内訳＝テンプレ11・カラー7（この時キャンバスは「写真・動画を入れる」の覆いで完全に隠れ、選んでも1pxも変わらない）／BGM8枚（音を乗せる相手が無い）／ステップタブ「編集・字幕・書出」3個（押すと「先に素材を追加してください」か押せない書き出しボタン＝**行き止まり3→0**）。あわせて iPhone に無い操作の言葉「画像/動画をドロップ」「タップ、またはドロップ」→「写真・動画を選ぶ／カメラロールから、その場で撮ってもOK」（**PC前提の言葉 1→0**・ドロップ機能自体は残す）、例文のOS標準絵文字1つ除去。機能は1つも削っておらず、写真2枚を入れると **22→60** に戻ってテンプレ・色・BGM・4タブ・タイムラインが全復帰（ポップ／暖色／書出を実際に押して確認）。素材を全消ししても「書出」タブに取り残されないよう `viewStep` で素材画面へ戻す。**横スクロール 0**・consoleエラー0（375px/430px 本番実測）。副産物＝同じ `themeHint` に繋がったテーマ入力欄と「AIに台本を書いてもらう」ボタンの**二重表示が解消**（ヒーロー側は素材0のみ・字幕タブ側は素材ありのみ）。
  - ⬜ 残り: ①書き出しをプラットフォーム別に1タップ（リール9:16／フィード4:5／ストーリー）＝比率と長さを自動で合わせる（現状 1080×1920 固定・`OUT_W/OUT_H` と `CANVAS_W/H` が描画全体に効くので要設計）②書き出し後の画面に押せるボタンが11個ある（ダウンロード／インスタで開く／字幕コピー／AI本文／Story文コピー／予約追加／予約一覧／友達に送る／別ver.／本文コピー／フック別案）＝主アクション1つ＋段階的開示へ ③ヘッダーの32×44アイコン3個（背景を変える・メニュー・戻る）が44px未満 ④WebM時の案内が「CloudConvert / HandBrake」＝スマホで実行できない復旧手段
  - なぜ気持ちいい: CapCutはスマホ単体で撮影→編集→書き出しまで完結するので、思いついた瞬間に出せる。Irisはリールスタジオまで来ても「PCでやった方が早い」箇所が残ると、その一点で流れが切れる。
  - どこにどう: iPhone 375px で**リールスタジオの全工程を実機通し**、①横スクロール暴発 ②44px未満のタップ対象 ③ファイル選択/書き出しでPC前提の操作 の3点を洗い出して潰す。**書き出しはプラットフォーム別に1タップ**（リール9:16／フィード4:5／ストーリー）＝比率と長さを自動で合わせる（入力を消す）。

### Prism（AI司令塔・ナレッジ・パープル#9333EA）
- ✅ **@で対象を指しての"実行まで"（Raycast の @メンション×MCP を移植）** — 2026-07-27 本番済。Cmd+K で `@` を打つと「@ナレッジ / @カレンダー / @メール / @売上 / 個別ノート」が出て、選ぶとチップとして残る。以降の依頼は**その対象の実データだけ**を読んで実行（`src/lib/mentionTargets.ts` が実データを取得 →`useAgentTaskQueue` の `contextText` として AI に渡す・画面には出さない）。繋がっていない連携は候補に出さない／読めなければ理由を出して止める（黙って一般論を書かせない）。よく使う依頼は対象の ID ごと保存＝2 回目以降は 1 タップで**最新データ**を読み直して実行。375px / 1440px 実機検証済
  - なぜ気持ちいい: Raycastは `@Notion` のように**対象を指してから頼む**ので、AIが何を見て何をするかが曖昧にならず、しかも実行まで一気に届く。Prismのコマンドバーは自由文のため「どこまでやってくれるのか」が分からず、結局オーナーが手で操作する。
  - どこにどう: コマンドバーで **`@` を打つと対象の候補（ナレッジ／カレンダー／メール／ナレッジの特定ノート）が出て、選ぶと入力欄にチップとして残る**。以降のお願いはその対象に限定＝AIの見る範囲が目に見える。実行できるものは**実行前に「これをします」1行確認→実行→取り消し1タップ**（silent fail 禁止・できない操作は「まだできません」と正直に出す）。
- ✅ **自分の道具箱になる「よく使う操作」の保存（Raycast の拡張＝育つ launcher を移植）** — 2026-07-26 本番済(d365efe)。Cmd+K の AI 依頼を prompt を鍵に自動保存し、空クエリで最上段＝入力ゼロ1タップ再実行。消す＋元に戻す付き。あふれ時は最古でなく最少使用を破棄。375px のタブ重なり(既存不具合)も同時に解消
  - なぜ気持ちいい: Raycastは使うほど自分専用に育ち、2回目以降がゼロ手数になる。Prismは毎回ゼロから文章を打つので、同じ依頼を何度も打ち直している。
  - どこにどう: 実行した依頼の直後に **「これをよく使う操作にする」1タップ**。保存するとコマンドバーを開いた瞬間、**入力ゼロで自分の操作が上位3つ**に並ぶ（＝正しいデフォルト・前回踏襲）。保存ゼロの人には出さない（空カードを見せない）。

## 競合から盗む（2026-07-23）
> 出典: Later（可視カレンダー＋グリッドプレビューで公開前に仕上がりが見える／ドラッグ&ドロップ）・CapCut（トレンド音源・テンプレで速く仕上がる）・Raycast（近似ゼロ遅延・入力を待って必要な結果だけ）・Notion AI（インライン編集で文脈切替を消す）。※体験の移植のみ・丸コピー禁止・嘘数字禁止。

### Iris（Instagram運用・ピンク#FF6B9D）
- ⬜ **公開前に"仕上がりが見える"グリッドプレビュー（Later のグリッドプレビュー移植）**
  - なぜ気持ちいい: Later は投稿予定を実際のIGグリッド並びで見せ、"世界観が揃うか"を公開前に確認できる安心がある。Iris はリール/投稿を作っても並びの見え方が分からない。
  - どこにどう: 予約・下書きを3列グリッドのモックに並べて表示（画像/リールのサムネ）。iPhone 375px で3列が潰れないよう正方形サムネ＋横スクロール無し（§0）。まず表示のみ。
- ⬜ **台本→リールの間に"トレンド音源テンプレ"を1タップ（CapCut の速さの移植）**
  - なぜ気持ちいい: CapCut はトレンド音源とテンプレで「考える前に形になる」。Iris は IrisFlowHub で台本は出るが、その後の"素材を音に乗せる"が分断（既存メモ project_iris_reel_pivot と接続）。
  - どこにどう: 台本カード下に「この尺（15/30/60秒）に合う構成テンプレ」を選ぶだけの3択チップ。手入力を消す（§0.5）。音源そのものの配信はしない＝構成尺の提案まで。
### Prism（AI司令塔・ナレッジ・パープル#9333EA）
- ⬜ **ナレッジを"その場でインライン編集"（Notion AI のインライン編集移植）**
  - なぜ気持ちいい: Notion は別画面に飛ばず記録をその場で直せて文脈が切れない。Prism のナレッジは開く→編集→戻るの往復で切替コストがある。
  - どこにどう: ナレッジカードをタップで別ページに飛ばさず、その場で展開して1タップ編集（楽観的更新＝§1 待ちを消す）。base64画像は永続化除外の既存ルール（memo project_prism_knowledge）を維持。
- ⬜ **コマンドバーの"入力を待って必要な結果だけ"（Raycast の集中の移植・7-05分の深堀り）**
  - なぜ気持ちいい: Raycast は打つ手を止めた瞬間に必要な結果だけ出し、雑音がない。Prism の検索/コマンドは候補が多いと迷う。
  - どこにどう: コマンドバーは入力停止まで結果を出さず、確定後は上位3件だけ＋"もっと"は隠す（段階的開示）。声入力ボタンを常設（§3 声の速さ）。

## 競合から盗む（2026-07-05）
> 出典: Raycast公式ブログ（a-fresh-look-and-feel／technical-deep-dive：コマンドバー・大きな検索バー・入力を待って必要な結果だけ・体感速度）/ 一般記事のNotion AI文脈。※Prism（AI司令塔・ナレッジ）向け。体験の移植のみ・嘘数字禁止。

- ⬜ **どこからでもコマンドバー**（Raycast「入力を待ち、必要な結果だけ出す」）：Prismのどの画面からもCmd+K（モバイルは大きな検索FAB）で「ナレッジ検索／提案を出す／アクション実行」を1本に集約。検索バーと先頭アイコンを大きく（走査しやすく）。迷ったら検索、が指に馴染む状態に。
- ⏭ **速さは機能（体感速度）**（Raycast「速く感じる細部」）：検索・提案を楽観的更新＋先読み＋スケルトンで「押した瞬間に出る」体験へ。索引は事前構築。遅さは不具合として直す。
- ✅(2026-07-05出荷) **知識→提案の自己強化を可視化**（Notion AI文脈）：ナレッジを足すほど提案が賢くなる感覚を、「◯件の知識をもとに提案しました」根拠チップで見せる（※実データのみ・0件なら出さない・嘘数字禁止）。足す→効く、の手応えを作る。
  - 実装: `src/lib/proactiveAgent.ts` の `generateProposal()` で、プロンプトに実際に織り込んだナレッジ件数(既存の`slice(0,5)`と同じ配列を数えるだけ・水増しなし)を`knowledgeUsedCount`としてProposalに格納。`src/types/identity.ts`にフィールド追加。`src/components/TodayBrief.tsx`で`knowledgeUsedCount > 0`の時だけ根拠チップ(BookOpenアイコン・テーマカラーの控えめborder+背景)を表示、0件/未計測時はチップごと非表示(嘘の演出なし)。coachBriefToProposal経由の提案はknowledgeUsedCount未設定のためチップ非表示のまま(正直)。tsc0・build成功・本番デプロイ確認済み(https://core-prism-app.vercel.app/ 200)。

## 競合から盗む（2026-07-05・Iris）
> 出典: Later公式比較・softtech-reviews（9マス・グリッドプレビュー＋ドラッグ&ドロップの創作感）/ CapCut公式・spliceapp（2本指ピンチで時間軸・選択中ハイライト・ワンタップテンプレ）。Canvaは今回未確認。※Iris（Instagram運用）向け。体験の移植のみ。

- ✅(2026-07-05出荷) **9マス・グリッドプレビュー＋並べ替え**（Later「投稿前にフィード全体の見た目が分かる」）：生成した投稿をIrisFlowHubで9マスに置き、公開前に統一感を確認→ドラッグで順番を入れ替え。「事務作業」でなく「フィードを作る創作」に感じさせる。
  - 実装: `src/iris/IrisFlowHub.tsx` に `FeedGridPreview` を追加。投稿予約（usePostQueue）の未投稿分を予約時刻の昇順で最大9件グリッド表示（9件未満は薄いプレースホルダー）。デスクトップはHTML5ドラッグ&ドロップ、モバイルはセルをタップ選択→44px矢印ボタンで並べ替え。並べ替えは対応する予約時刻(scheduledAt)を入れ替えて反映、保存失敗時はエラー表示＋再試行を提供（silent fail無し）。`IrisDashboard.tsx` から `postQueue` を橋渡し。tsc/build green、本番デプロイ確認済み(https://core-prism-app.vercel.app/iris 200)。
- ⬜ **2本指ピンチで時間軸ズーム**（CapCut「ピンチで全体↔精密」）：予約カレンダー／台本タイムラインを2本指ピンチで週↔日に切替。選択中は視覚ハイライト。多層メニューに頼らず指で直接いじる手触りに。
- ✅(2026-07-10出荷) **ワンタップ・テンプレで“開いて即出す”**（CapCut「型に流し込むだけ数タップ」）：台本→リールを穴埋めテンプレ化し、素材を落とす→文言を直す→数タップで完成。白紙から作らせず、最初から“もう形になっている”状態に。
  - 実装: `src/iris/reelTemplates.ts`(新規)に実証6型(Before→After/知っておくべき3選/1日ルーティン/お客様の声/やりがちな失敗/開封お披露目)を静的定義。各型は hook→build→payoff→cta の各カットで「撮るもの・字幕たたき台・秒数」＋本文スケルトン(差込{ })＋ハッシュタグを持つ。`src/iris/IrisReelComposer.tsx`の空の状態(素材0件&未構成)に型ピッカー(2列グリッド)を追加。選ぶと撮影設計図＋本文の型を表示、ワンタップコピー(クリップボード不可でも落とさない)、「この型で撮った素材を入れる」で既存のアップロード→AI構成へ橋渡し。LLM不使用・即表示・失敗ゼロ。tsc0・build緑・push(a690a29)本番反映。

## フラッグシップ三本柱 出荷（2026-07-02）
### Prism — 高級コンシェルジュ・ウィジェット
- ✅ /concierge 新設: グラスモーフィズム(blur24px)チャット＋Living Portrait抽象美形アバター(idle呼吸/listening/thinking/speaking・原価¥0・heygen/did差し替え口あり)＋敬語コンシェルジュ(/api/ai)＋リード獲得→/api/feedback。ショーケース=CSS高級不動産風ヒーロー＋設定フォーム→その場プレビュー→1行埋め込みタグ生成。public/prism-concierge.js(iframeローダー・postMessage開閉)。vercel.jsonで/conciergeのみframe-ancestors *(他パス防御維持)。価格カード=Standard ¥29,800+初期¥98,000/Luxury ¥49,800+初期¥298,000
- ✅ リード通知の二重化: /api/feedbackのResend失敗時(本番でmail_failed実測)→/api/mail/gmailのlead_notify(宛先オーナー固定・replyTo訪問者)へ自動フォールバック。本番実測 delivered:true reason:gmail_fallback ＝機会損失ゼロ
### Iris — 思考を投げるだけ
- ✅ ホーム最上部にIrisThoughtDropSection: 巨大入力＋音声(useVoiceInput・波形パルス)→1リクエストでX/Instagram/note並列生成→実物風カード(X文字数カウンタ/IGハッシュタグチップ+リールフック/noteエディトリアル)→編集/コピー/ベスト枠に1タップ予約(usePostQueue+suggestNextSlot)。エラー時は必ず再試行ボタン
- ✅ IrisInsightSummary「今週のあなた」: インクブラック→ピンクグラデ1枚カード。生んだ投稿/予約済み/連続日数=全て実データ(honest-numbers)、0件時は励まし空状態
- 検証: tsc0・ビルド緑・本番デプロイ・smoke5/5・実生成E2E(思考→3カード→予約キュー登録)・コンソールエラー0

## 競合から盗む（2026-06-30）
> 出典: Iris=later.com公式（Quick Scheduling・Visual Planner）/ heropost.io・aiproductivity.ai（Later vs Buffer 2026）/ buffer.com（最適時間）。CapCut未確認。Prism=raycast.com公式・falconnotes.com（Raycast クイック・キャプチャ／クリップボード履歴）。Mem・Superhuman未確認。※先週のLaterグリッド／Bufferバリエ／Canvaシーン、Prismの音声・根拠チップは別項のため重複回避。

### Iris
- 🟡(最小スライス2026-06-30 下書きをベスト枠に一括予約=ドラッグ無し版) **最適枠を先に置いた一括ドラッグ予約**（Later「Quick Scheduling」）：カレンダーに「よく伸びる時間帯」の空き枠をあらかじめ薄く表示しておき、生成したリール台本/サムネを枠へドラッグ→**まとめて予約**。1本ずつ日時を手入力する手間を消す（§0.5「入力を消す」）。枠はフォロワーの活動推定から提案（数字が無い時は“推定”と明記し嘘を出さない）。並べ替え=即反映の手触りはLater項と共通。
- ✅(2026-06-30出荷・IrisBestTime) **最適投稿時間のおすすめ表示**（Buffer「フォロワー活動ベースの最適時間」）：投稿予約時に「この曜日のこの時間が狙い目」を控えめに提案し、1タップでその枠に入る。Irisが連携データを持つときだけ実データで、無いときは一般的な目安と断る（honest-numbersルール）。考える手間を1つ消す。

### Prism
- ✅(2026-06-30出荷・PrismQuickCapture) **どこからでも一瞬で知識に放り込む“クイック・キャプチャ”**（Raycast「Quick Capture」）：画面右下に常設のミニ入力（テキスト／URL貼付）を置き、開いている作業を中断せず1〜2タップで知識に保存→あとで仕分け。「あとで書こう」で消える気づきをゼロ摩擦で貯める＝「貯める→効く」ループの入口を太くする。保存時に楽観的更新で“もう入った”感を即返す（待ちを消す）。
- ⬜ **貼り付け履歴から知識化**（Raycast「クリップボード履歴」）：直近コピーした数件を候補チップで見せ、選ぶだけで知識カード化。毎回ペーストし直す手間を消し、会議メモやリンクの取りこぼしを防ぐ。履歴はこの端末内のみ・期限つきでプライバシー配慮、本文は持たず候補表示だけ。

## 競合から盗む（2026-06-29）
> 出典: Iris=postquick.ai・max-productive.ai（Later）/ themarketingagency.ca・buffer.com・socialrails.com（Buffer）/ canva.com・aiworthit.com（Canva）。CapCut未確認。Prism=omerh.me（Raycast）/ willowvoice.com（Notion AI 音声）/ eesel.ai・max-productive.ai（Notion AI）。Mem・Superhuman未確認。

### Iris
- ⬜ **リアルタイム反映のグリッドプレビュー**（Later ビジュアルプランナー）：IrisFlowHubの「リール台本→投稿」段に、生成サムネが並ぶプロフィールプレビュー帯を置き、台本カードを上下ドラッグすると公開順プレビューが即更新。「投稿したらどう見えるか」を想像でなく目で見て直せる。ピンクグラデの既存トーンで、並べ替え=即反映の手触りだけ移植（※2026-06-26のLater項と連動）。
- 🟡(最小スライス2026-06-30 尺15/30/60秒の選択生成) **1企画→横タブで複数バリエーション同時生成**（Buffer コンポーザー）：scriptStudioで1つの企画から「フック違い」「尺違い（15/30/60秒）」「トーン違い」を横タブで同時生成、各タブにそのまま撮影できる台本を出す。運用代行が複数案を即比較→そのまま現場に渡せる。再生成は媒体／尺タブ単位で。
- ✅(2026-08-04 本番済) **シーン単位ブロックを掴んで並べ替え**（Canva 動画エディタ）：リール台本を「シーン単位」のブロックに分け、各ブロックに参考画像／Bロール案をドラッグ割り当て、ブロック自体も入れ替え可能に。撮影者・編集者がカードを見れば即動ける状態へ。Canvaの見た目でなく「シーンを掴んで並べる」操作だけ移植。
  - 実装: ブロック化・並べ替え・秒数編集は `IrisReelDirector.tsx` に既にあったので、残っていた**2つの穴**を塞いだ。①**Bロール案がどのカットのものか分からない**（台本の下に平たいリストがあるだけ＝撮影者は「どこで撮るのか」を自分で推測していた）→ `ReelCut.broll` を新設し、選択中カットに素材案をタップで割り当て。カット表(`projectToCutSheet`)・台本の編集メモ(`projectToShots`)の両方に載るので、どの書き出しから見ても同じ内容になる。未割り当ての残りもカット表の末尾に必ず出す（黙って落とさない）。タイムラインのタイルには「素材n」の印。②**再読み込みで台本ごと全部消えていた**（state のみ・保存ゼロ＝生成し直しから、カット編集もやり直し）→ `reelDirectorStore.ts` を新設し端末内へ自動保存（手が止まって500ms後に1回・中身が変わった時だけ書く）。`IrisScriptStudio` も保存から台本を復元するので、閉じても生成し直しが不要。保存に失敗したら黙って落とさず「閉じると消える」と出してカット表のコピーを促す。「台本どおりに戻す」で編集破棄も可能。
  - ドラッグでなくタップ割り当てにした理由: 375px でタイムラインが横スクロールする中でのドラッグは掴み損ねが多く、Canva の価値は「掴む操作」でなく**シーンとその素材が1枚のカードで揃う**ことなので、モバイルで確実に動く方を採った。
  - 判定（375px 実測）: 素材案を割り当て→タイムラインの印が「素材1→素材2」、候補が「残り3件→2件」、その場で端末内保存（localStorage の savedAt 更新を実測）。**再読み込み後も割り当てが残る**ことを実測（以前は台本ごと消滅）。復元時の表示は保存時刻をそのまま出す（開いた時刻に化けないよう、中身が変わるまで書かない）。tsc 0・vitest **78件緑**（`reelDirectorStore.test.ts` 11件を新設＝壊れた保存/古い版/カット0件で落ちない・テンプレ再適用で割り当てが消えない・書き出しに必ず載る を固定）・build緑・横スクロール0(docSW=375)・タップ44px・consoleエラー0

### Prism
- 🟡(2026-06-30 学習する初期表示=頻度順を実装) **学習する初期表示＋行ごとに次の一手が見えるコマンドバー**（Raycast）：コマンドバーを開くたび「最近使った提案・知識・操作」を学習順で初期表示。各候補行にアイコン＋一言の文脈（例「3日前の会議メモ」）を添え、右に「→で開く／⌘Kでアクション」。覚えなくても迷わず、待たせず「考える→実行」を地続きに。パープルのまま体験だけ移植（単一入口の柱と連動）。
- ⬜ **長押し音声→その場の操作へ直結**（Notion AI 音声入力）：コマンドバーと提案カードにマイクを置き、長押し中だけ波形→離した瞬間に文字化し、その場の操作（提案を出す／知識に保存／書き換え）へ直結。Notionが弱いスマホ音声入力こそPrismのキラーUXに。短い指示は打つ方が速いので音声は「長い複雑な指示」用と割り切った導線。
- ✅(最小スライス2026-06-27 知識ゼロの空状態を「貯める→効く」を伝える触れる入口に。次=根拠チップ/このメモから提案) **知識とAI提案を同一面で・根拠チップ表示**（Notion AI インライン）：知識を貯める画面とAI提案を別タブに分けず、知識カードの隣に常設の「このメモから提案する」ボタン。提案には必ず「どの知識を根拠にしたか」をチップ明示し、貯めた量が増えるほど根拠数が増える表示で「貯める→効く」自己強化ループを可視化。

## 抜本的に使いやすく（最優先・2026-06-26）
### Iris
- ✅ 下部ドック「その他(≡)」を全機能シート直結に(2026-07-23 0660f05)。旧: ≡アイコンなのにホーム遷移で、リール以外16機能が上部横スクロールナビからしか開けなかった(死んでた`onMore`配線を接続)。→ 親指の位置から1タップで全機能到達。375px実機で開閉・ホーム遷移・aria-label確認/tsc0/build緑
- 🟡 手入力を消す：連携データから戦略・台本・投稿案を自動生成、ユーザーは選ぶ/直すだけ。白紙を見せない
  - ✅(2026-07-25 aedf1f4) ホーム最上部の巨大入力(IrisThoughtDrop)の白紙化を解消。空欄時のみ「書き出し」チップ5種(今日の気づき/おすすめ/本音/よく聞かれる/失敗談)を表示→1タップで本人が続きを書ける stem を差込・カーソル末尾。文字入力で即消滅。375px 2段折返し・tap40px・honest(代筆せず本人が書く)。E2E実測OK・本番alias済
  - 残: 連携済ユーザーには連携データから“もう埋まった”下書き（案件文/台本/投稿案）を自動生成して見せる本丸へ。
- ✅(2026-07-27 本番済) 〔UX観察〕ThoughtDrop本体がフルスクリーンIRISヒーローに押し下げられ、ホームで折返し外に沈む → **入力を先頭・花はそのすぐ下**に並べ替えて解決。
  - 実測(本番 375×812・下部ドック上端=748): **Before** 入力カード y=669〜1119＝折返しの外。見えていたのは入力欄の上半分57px(全117pxの49%)だけで、「3つの投稿に変える」も「声で投げる」も画面外＝スクロールしないと投げられない。**After** カード y=215〜490＝**全部が1画面の中**。入力欄・マイク・送信・書き出しチップ5個すべてが0スクロールで見えて押せる(elementFromPointで実測・最小タップ44px)。
  - 減らしたもの: **入力カードの高さ 450px→275px(−39%)**。①マイク(64px円+説明1行91px)と送信ボタン(52px)を**1行に統合**、②平常時の「タップして、声で投げる」を消す(録音中/文字起こし中/エラー/非対応の時だけ出す＝黙って失敗しない)、③書き出しチップを2段折返し(96px)→**1行の横スクロール(44px)**にして見出し「書き出しから選ぶ」を削除。**縦ブロック 5→3**。
  - 花は縮めていない(min(46svh,480px)/minHeight300)。花を上に残したまま縮める案も測ったが、右下「AIと話す」(y=614〜)と左の切替オーブ(y=596〜)が**必ず送信ボタンに被る**ため成立せず、順序変更を選択。`IrisBloomStage` として切り出し、`IrisThoughtDropSection` の `stage` プロップで入力の直下に描画。
  - 併せて修正: 1行nowrapにしたことで grid の既定 `auto` 列が max-content に伸び、**カードが335px→493pxに広がって画面右へはみ出した**(実測)。ヒーロー/セクション/入力の3つの grid を `minmax(0,1fr)` に固定して根治。本番 docSW=375＝横はみ出しゼロ。
  - 検証: tsc該当0／vite build緑／`/iris` `/prism` `/` `/corp` 全200／リールタブ退行なし／コンソールエラー0。
- ✅ 2026-08-18 **浮きボタンが「押せるもの」の上に乗らない配置ルールを全プロダクト共通で決めた**（`80462bf`・本番反映済）：本番 iPhone 13(390×664)で実測したところ、**押せるものが 3 個、浮いた丸ボタンの下に埋まっていた**。①Prismランディングのマイクボタン（音声でタスク予約）が主ボタン「3日間 無料ではじめる」を 888px²、「料金を見る」を 1,576px²＝**お金の入口を合計 2,464px²** ②Iris「動画おまかせ」の「AI と話す」がカードの主ボタン「任せる →」を 2,778px² ③同画面で切替オーブが表題「クリエイティブ司令塔 / THE CREATIVE DIRECTOR」を 1,054px²。**見えているのに押すと別のものが開く**＝いちばん質の悪い壊れ方。原因は2つで、(1)「AI と話す」「音声でタスク予約」には**避けるしくみが最初から無かった**（CSSで隅に固定しているだけ）(2)切替オーブは避けるしくみを持っていたが、**タブ切替では測り直していなかった**＝タブ切替はスクロールもリサイズも起こさないので、既存の合図（settleタイマー・resize・スクロール停止・帯のResizeObserver）に**一つも当たらない**。加えて重みが 文字3 : 押せるもの10 だったため「文字を少し避けるためにボタンの上に乗る」が起き得た。**直し**＝`src/lib/floatAvoid.ts` を新設し、ルールを1か所に集約：**押せるもの（ボタン・リンク・入力）の上には絶対に置かない**（重み 10000:1＝実質の禁止／文字は「なるべく」避ける＝文字で埋まった画面でも破綻させない）・**逃げ道は同じ側の縦の道だけ**（左右に飛ばすと指が覚えた場所が変わる）・**中身が入れ替わったら測り直す**（MutationObserver・320msでまとめ・自分の枝の変化は無視）・**いま何も覆っていなければ動かさない**（読んでいる最中に飛び回らせない）。測るのは要素の箱ではなく**文字が実際に描かれている行の箱**（CoreDock 2026-08-01 の知見を踏襲）。`hooks/useFloatAvoid` を SupportChat（AI と話す）と PrismTaskScheduler（音声でタスク予約）へ適用、CoreDock には MutationObserver での測り直しと重み修正を追加。**スマホ（≤900px）だけ**に効かせ、デスクトップ1280pxは marginBottom 0px のまま＝実測で位置変化ゼロ。**実測 Before→After**＝Prism LP マイク 押せるもの2,464px²→**0**(y=528→424)／Iris 動画おまかせ「AI と話す」4,038px²(うち押せるもの2,778)→**577px²・押せるもの0**／同オーブ 1,054px²→**0**(y=192→448)／Iris 予約投稿「AI と話す」1,442px²→**0**。**覆われていた押せるもの 3個→0個**。検証＝`tsc -b`緑／`vite build`緑／dist実配信＋Playwright(iPhone 13)で4タブ実測／コンソールエラー0／docSW=390（横スクロールゼロ）／本番URLで再実測。注：Iris「動画おまかせ」に残る 577px² は本文の文字の上（**この画面には一つも空いている場所が無い**ため最小を選んだ結果）で、押せるものは0。
  - ⬜ 残り: **Prismランディングのダークモード切替が header の「無料でためす」を 338px² 覆う**。これは本文の上ではなく**固定どうしの衝突**なので、今回の「縦に逃がす」やり方では直らない（ヘッダー側で場所を空けるのが筋）。
- ✅ IrisFlowHubをホーム化(連携済みは1本道プランを最上部・0スクロール、重複ヒーロー2枚=エディトリアル/稼ぐは連携後は非表示で7→5セクション化 2026-06-29)。分析→戦略→リール→稼ぐを「次へ」で
- 〔次〕簡易モード(simple_mode)のコマンドバー版ホームにも同じ1本道プランを出す／タブ26個を機能グループで段階開示し常時表示を減らす
- ✅ 専門用語をやさしく(ER→反応率, IrisStrategyHome/IgConnectModal済 2026-06-27)。意味が分からない画面をゼロに
### Prism
- 🟡 単一入口「話すだけ/書くだけ」：7つの柱をコマンドバー1つに集約し迷いを消す ✅(2026-06-30 モバイル入口=コマンドバー(⌘K・CommandPalette・50+コマンド/CXO/AI委譲)はスマホで hidden md:flex のため到達不能だった→左下に常時見えるFAB「探す・操作する」48px/safe-area/右下SupportChatと非重複 を追加。tsc0・Viteビルド緑・/200・alias●Ready。残=簡易モードのコマンドバー版ホーム/学習初期表示)
- ✅ 初回スプラッシュの強制待ちを2.8s→1.9s短縮＋スキップ案内を最初から表示(待ちを減らす 2026-06-27)。次:空っぽ解消/賢いデフォルト


## Iris 競合から盗む（2026-06-26 競合分析）— Later/Buffer/Canva/Edits/CapCut
- ✅(最小スライス2026-06-27 リスト⇄グリッド表示切替を実装。3列サムネで一望。次スライス=ドラッグ並べ替え) 【効果大】ビジュアル・フィードプレビュー（Later 9枚グリッド）: IrisPostQueueView に3列グリッド表示モード＋framer Reorderで並べ替え→scheduledAt入替。空きは点線「ここに追加」
- 🟡(2026-07-23 カレンダー表示を出荷・e7bb0d4) ドラッグ＆ドロップ・カレンダー（Buffer）: scheduleタブに月カレンダー表示を新設（リスト/グリッド/カレンダーの3切替）。`IrisPostQueueView.tsx`にview='calendar'追加＝月グリッドにpostsByDay（localDayKeyでローカル時刻集約・ISO slice の+9hズレ回避）をマップ、件数を状態色ドット表示、日タップでその日の予約を下に展開（投稿/コピーの実アクション付きコンパクトカード）。きょう/選択日/土日を色分け・前後月ナビ。tsc該当緑・build緑・本番/iris200。残スライス=空セルへのsuggestNextSlot薄提案＋セル間ドラッグで予約日を移動
- ⬜ 【効果大】リール維持率＋スキップ率の即時可視化（Instagram Edits）: 実績カードに横軸秒・縦軸維持率のSVG折れ線＋「3秒で○%離脱→冒頭1秒に結論」。実データのみ(honest-numbers厳守)
- ✅(最小スライス2026-06-29 SRT書き出し) ワンタップ自動字幕（CapCut/Edits）: 撮影台本（②台本）の各ショットの time＋テロップ/セリフから標準 **SRT字幕** を生成する `scriptToSrt`（テロップ優先・実データのみ＝honest、文言は作らない）。台本表示に「字幕を作る（CapCut/Edits用）」ボタン→クリップボードへ→CapCut/Editsの「字幕を読み込む」に貼れる。tsc0・Viteビルド緑・/iris200・alias●Ready確認。次スライス=動画への焼き込み（ピンク縁取り3プリセット）はReelStudio側で。
- ✅(最小スライス2026-06-29 テンプレ起点) テンプレ起点1画面エディタ（Canva）: IrisScriptStudio ①企画の上に「今日のテンプレ」横スクロール（悩み解決/ビフォーアフター/◯選/誤解/失敗談/初心者/比較/1日/Q&A/チェックリストの10種）。タップでフォーカス欄を埋め（再タップで解除）、AIがクライアントのジャンル・人物像に合わせて具体ネタへ展開＝白紙ゼロ・打鍵ゼロで企画開始。chips 36px・スクロールバー非表示。①企画・②台本ともに本番化。①tsc0・Viteビルド緑・/iris200・alias●Ready。②台本2026-06-29追加=②台本の自由入力上に「台本の型」横スクロール8種（結論先出し3ステップ/ビフォーアフター/ストーリー/誤解/比較/Q&A/チェックリスト/失敗談）。タップでfreeTopicを埋め→AIが本格台本へ展開。tsc0・Viteビルド緑・/iris200・alias●Ready確認。残スライス=口調切替の即プレビュー。

## Prism ユーザーフィードバック対応（2026-07-06）
- ✅ 「真ん中下のチャットが表示されたまま下に収まる待機ボタンが欲しい」（実テスターの声）→ BottomChatDock.tsx にchevron「待機」ボタンを追加。押すと入力バー全体が小さな帯ピル（`{name} に聞く`）に畳まり下のコンテンツが見えるスペースを確保、タップで復帰。localStorage(`prism-chat-dock-minimized`)で状態記憶。既存の送信/展開ロジックは無変更。tsc0・build緑・本番alias済・/200確認。

## 2026-07-14 オーナー指示（主力集中: Prism/Resonance）
- [x] レシートOCRを前面へ（クイックアクション2番目・primary昇格・ラベル「レシート整理 OCR」）
- [x] クイックアクション 31→24 に削減（YouTube要約/交渉練習/迷い整理/同業比較/人を気づかう/アプリ操作代行/重複ノートを撤去。モーダル自体はCommandPaletteから到達可）
- [x] note記事: 文字量チップ+スライダー（300-6000字）＋見出し画像自動生成＋X/Threads告知文を同時生成
- [x] 画像生成: 用途別（YouTubeサムネ/インスタ/ストーリー/X/note/バナー）同時生成
- [x] 動画生成: video-gen フィーチャーキー新設、最上位プラン（studio/agency/enterprise）限定にゲート
- [ ] ★次: Apple Watch連携のシームレス化 — ショートカット設定を1タップ導線に（プロファイル自動生成）、同期状態の常時表示＋途切れ検知の通知、HealthSnapshotへの自動反映確認バッジ
- [ ] Crystalは高価格帯のため当面は「Prism/Resonanceの後ろの導線」に位置づけ（新機能開発は保留）
