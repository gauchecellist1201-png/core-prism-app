// ============================================================
// CORE Studio — 配色とフォント (StudioSite / FilmTab で共有)
// ============================================================

// ---- 白基調・法人トーン ----
export const C = {
  bg: '#FFFFFF',
  alt: '#F7F7F5',                 // 交互セクション
  ink: '#111827',                 // 見出し・強調
  body: '#374151',                // 本文
  mute: '#6B7280',                // 補足
  line: '#E5E7EB',                // 罫線
  gold: '#A8823C',                // アクセント (線・面のみ。文字には使わない)
  // #A8823C は白地で 3.55:1 しかなく文字が読めないため、文字用に濃い金を分けている。
  // 交互セクションの #F7F7F5 の上でも 4.5:1 を割らない濃さにしてある (#8f6d2f では 4.45:1 で不足した)。
  goldText: '#8a6a2d',            // 白地 5.02:1 / #F7F7F5 上 4.68:1
  goldLine: 'rgba(168,130,60,0.4)',
  dark: '#111827',                // CTAボタン
};

// ---- 暗部 (映像の章)。白地用の金は暗地で沈むため、文字に使える金を別に持つ ----
export const D = {
  bg: '#0B0B0C',
  raise: '#141416',               // 暗地の上に置く面
  ink: '#FFFFFF',
  body: '#D8D8DC',                // #0B0B0C 上で 13.9:1
  mute: '#A0A0A8',                // #0B0B0C 上で 7.4:1
  line: 'rgba(255,255,255,0.14)',
  gold: '#D4A94F',                // #0B0B0C 上で 8.5:1 (暗地では文字にも使える)
  goldLine: 'rgba(212,169,79,0.38)',
};

export const SERIF = '"Noto Serif JP", "游明朝", "Yu Mincho", serif';
export const SANS = '"Noto Sans JP", "Inter", sans-serif';
